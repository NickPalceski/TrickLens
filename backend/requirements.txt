# Pinned to minor versions: reproducible builds without freezing out patches.

# --- Web ---
fastapi==0.115.*
uvicorn[standard]==0.32.*
# Adapts the ASGI app to Lambda's event/context calling convention.
mangum==0.19.*
pydantic==2.9.*
pydantic-settings==2.6.*

# --- Database ---
sqlalchemy[asyncio]==2.0.*
asyncpg==0.30.*          # async driver, used by the app
psycopg[binary]==3.2.*   # sync driver, used by Alembic only
alembic==1.14.*

# --- AWS ---
boto3==1.35.*

# --- Dev tooling ---
# Shipped in the image so `make fmt` and `make test` need no host Python.
ruff==0.7.*
pytest==8.3.*
pytest-asyncio==0.24.*
httpx==0.27.*
